Java에서 인터페이스의 기본 메서드를 정의하기 위한 키워드는?&1. public$2. default$3. static$4. abstract&2
Java에서 예외를 전달하기 위해 사용하는 키워드는?&1. throw$2. throws$3. catch$4. finally&2
Java에서 프로그램과 외부 매체 사이에 데이터를 주고 받는 연결통로로서 역할을 하는 것은?&1. 트림$2. 스팀$3. 스트림$4. 스트리&3
Java에서 메서드 오버로딩과 오버라이딩의 차이점은?&1. 오버로딩은 같은 이름의 메서드를 여러 번 정의하는 것, 오버라이딩은 상위 클래스의 메서드를 재정의하는 것$2. 오버로딩은 상위 클래스의 메서드를 재정의하는 것, 오버라이딩은 같은 이름의 메서드를 여러 번 정의하는 것$3. 오버로딩은 매개변수가 다른 메서드를 정의하는 것, 오버라이딩은 매개변수가 같은 메서드를 정의하는 것$4. 오버로딩은 메서드를 상속하는 것, 오버라이딩은 메서드를 구현하는 것&1
Java에서 제네릭을 사용하는 주요 이유는?&1. 코드의 재사용성 증가$2. 컴파일 타임에 타입 검사를 통해 타입 안전성을 보장$3. 실행 속도 향상$4. 1과 2 모두&4
Java에서 컬렉션을 순회하기 위한 올바른 방법은?&1. for-each 루프$2. Iterator 사용$3. Stream API 사용$4. 위 모두&4
Java에서 다형성을 구현하기 위한 키워드는?&1. extends$2. implements$3. polymorphism$4. override&1
Java에서 스트림의 특징이 아닌것은?&1. 단방향$2. LIFO$3. FIFO$4. 시간지연은 발생될 수 있다&2
Java에서 기본 예외 클래스를 상속하는 클래스는?&1. Exception$2. Throwable$3. Error$4. RuntimeException&1
Java에서 쓰레드 생성을 위한 인터페이스는?&1. Runnable$2. Callable$3. Thread$4. Executor&1